spring:
  cloud:
    config:
      server:
        git:
          uri: file://${HOME}/Dev/workspace/Leandro-Labs/spring_config_repo/
          search-paths: /{application}
  profiles:
    active: local


